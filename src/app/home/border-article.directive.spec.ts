import { BorderArticleDirective } from './border-article.directive';

describe('BorderArticleDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderArticleDirective();
    expect(directive).toBeTruthy();
  });
});
